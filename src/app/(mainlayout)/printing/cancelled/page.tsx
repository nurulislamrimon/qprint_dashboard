import CancelledHome from '@/components/printing/CancelledHome';
import React from 'react';

const Cancel = () => {
    return (
        <div>
           <CancelledHome />
        </div>
    );
};

export default Cancel;